hosts-systems                          using host-generic.cfg which has check_host_alive command. We have this alert in Datadog which is up/down alert
host-parkplace                         using host-generic
hp-ilo5                                Calling check_ilo2_health.pl script with "3" argument which I think to use ilo3. Probably 3,4,5 are all supporting same method
hp-ilo4                                Calling check_ilo2_health.pl script with "3" argument which I think to use ilo3. Probably 3,4,5 are all supporting same method
hp-ilo3                                Calling check_ilo2_health.pl script with "3" argument which I think to use ilo3. Probably 3,4,5 are all supporting same method
linux-snmp                             Checking disk, cpu, logfiles, networkusage, memory, load, uptime, currenttime. we already have those
puppet                                 check_snmp_puppet using check_snmp_puppet.sh script which is checking if the puppet is running using snmp. There are 2 services puppet and master
puppet-master                          Uses check_connectivity.py with 8140 option. Checks the socket connectivity on port 8140 which is puppet master code.
ssh                                    Checks if SSh service is responding. One of the Nagios plugins
ssh-multi                              Uses same check_ssh nagios plugin but with multiple ports
ssh_2222                               Uses same check_ssh nagios plugin but for different port which is 2222 used by VMs which has a container that uses 22 port
rabbitmq                               Aliveness, Cluster, Connections, Exchange, Overview, Queue, ServerHealth services.
http                                   Simple web check for the given url and port. It also supports basic authentication. This is a whole pack with others. There are 11 services which are certificate, certificate_auth, http, http_expect, http_expect_py, http_servicenow_midserver, http_servicenow_midservers_unresolved, https, https_auth, htps_expect, https_multi
http-expect                            Simple web check with expected error codes(by default 403) as HTTP OK
http-expect-py                         same as above but with custom python script
https                                  Using same nagios plugin with -S option.
https-multi                            Using same http check for multiple ports
https-expect                           Same as http-expect but with -S option
http_servicenow_midservers             Part of http pack but using check_snow_midserver_status.py script. It checks the status of the SNOW midservers.
http_servicenow_midservers_unresolved  Part of http pack but using check_snow_midserver_status.py script. It checks the unresolved status of the SNOW midservers.
apache-httpd                           Queries the server-status page from an apache server and check the output. Uses check_apache_status.py file. There is only 1 service
mongodb                                There are connection, flush_time, index_miss_ratio, last_flush, lock_time, open_connections, replication_lag, replicaset services. Uses shinken_packs/check_mongodb.py
dns                                    2 service 1 for dns check and 1 for dns_query. dns is using nagios check_dns plugin dns_query is using check_dig nagios plugin
bind9                                  Only 1 service running check_snmp_dns_utilization script which is checking if the bind service is up or not.
terminal-arbiter                       defined in shinken pack but using terminal-monitoring pack as well for broker and arbiter. There are 3 services on terminal-monitoring pack; 1 using check_terminal_monitoring_pop_latency.py file and other 2 using datagatherer service
terminal-monitoring-number             using the pop latency script above
terminal-arbiter-no-graphite-check     using the pop latency script above
windows                                This has RDp(checking if rdp is available), processes, cpu, disk, disk-io,application logs, system logs, inactive sessions, load, interface, memory, uptime, services, swap etc. It's like linux-snmp but for windows. I think we don't need it as well
shinken                                Shinken pack which has 3 services; PollingLatency, ShinkenDaemon and arbiter. excep polling others use the same check_shinken.py file which basically sending request to /ping and checking if the service is alive or not.
shinken-arbiter                        included in shinken pack which is using the same script with "arbiter" option
shinken-scheduler                      included in shinken pack checking the latency and service
ftp                                    Has 1 service which is ftp and using check_ftp nagios plugin which is checking if ftp service is up
terminal-poller                        Same as shinken service. It's a pack and checking poller and receiver services if they are running using check_shinken.py
smtp                                   has 3 service, smtp, smtps, smtps_certificate. All using same check_smtp nagios plugin with providing options. It tests whether an SMTP server is up and running by establishing a connection and sending a simple handshake (EHLO or HELO), checking the response from the SMTP service.
ges-to-pops                            pack which has AMS, FRA, GLS, IST1 and THE services. It has only check_ping nagios plugin command which is checking the latency for the given ip
ges-to-IST                             Part of above pack with different thresholds
avanti-dbm                             Pack which has 1 service using check_bwpm_update.py script that as far as I understand checking the shapers?
proxysql                               pack which has 7 service to check proxied mysql conn, proxysql hg-10, hg-20, proxysql status, proxysql-rules, proxysql-connections. Uses check_proxysql and shinken_packs/check_mysql_health scripts.I think we already have this
mysql                                  pack which all kind of mysql checks using shinken_packs/check_mysql_health perl script. I think we already have this
mysql-avanti-custom                    using above pack with different threshold values. I think we already have this
mysql-slave                            host definition of mysql pack but for slave node. I think we already have this
trap-sender                            pack with 1 service which is TRAP. Uses clear_trap command.
oss-frontend                           pack with 6 service which uses check_oss_status_py3 and check_oss_status scripts. Jason knows better. We can move this to datadog
cfssl-ca-certificates                  Already done with customer_cert_expiry.py
emc-clariion-spa                       Belongs to emc pack which has disks, cache, faults, fc_ports, spa and spb, trespass services which are using check_emc_clariion.pl script with A as sp id
emc-clariion-spb                       Belongs to emc pack which has disks, cache, faults, fc_ports, spa and spb, trespass services which are using check_emc_clariion.pl script with B as sp id
emc-vnx-block-spa                      Same as above 2 except it doesn't have disks service and using A as sp id
emc-vnx-block-spb                      Same as above 2 except it doesn't have disks service and using B as sp id
emc-xtremio                            Different pack which has Faults and Performance services. Uses check_emc_xtremio.py script with different arguments
infoblox                               packs which is checking transfer count(uses snmp generic script), failure, nxdomain, nxrrset, recursion, referral and success zone stats. Uses check_infoblox_zone_stats.py with different argument. If necessary, I can add these metrics to the existing infoblox check
ntp                                    Packs only checks the ntp jitter to check if there is any time offset
juniper-ex                             For storage switches since managed by us GHY-Storage-SW01A, GHY-Storage-SW01B, GHY-Storage-SW01C, GHY-Storage-SW01D. Basic network device checks like cpuload, cpu temp, fpc, pic, mem buffer, fans, psu... Uses check_snmp_juniper_switch.pl script. It can easily moved to datadog
redis                                  pack which only has 1 service which is BgSaveStatus. USes check_redis_statistic_variables command. Uses check_redis.pl script.
redis-persistent                       part of above pack which has the same service.
